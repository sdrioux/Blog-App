.wrapper
  %h1=@post.title
  %h3=@post.author
  %p.blogtext=simple_format(@post.body)
  -if @post.picture_url !=nil
    =image_tag @post.picture_url.to_s, class: "blog-pic"

  .links
    = link_to 'Home', posts_path
    |
    = link_to 'Edit', edit_post_path(@post)
    |
    = link_to 'Delete', @post, method: 'delete'